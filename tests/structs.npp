#include <stdio.nh>
struct Contador {
    int valor;

    void incrementar(int n) {
        valor += n;
    }

    int get() {
        return valor;
    }
}

void main() {
    Contador c;
    c.valor = 0;
    c.incrementar(5);
    std::print(c.get());   // 5
}